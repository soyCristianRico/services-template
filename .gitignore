*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/.claude/settings.local.json
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# MCP server config: stays local because it can carry per-machine tokens.
# .mcp.json.example IS committed — it is the shared baseline every site starts
# from, and new-site.sh copies it into place. Add secrets only to .mcp.json.
.mcp.json
